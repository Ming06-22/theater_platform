create_tables.py: create a postrgresql database tables to store all the information
insert_tables.py: insert infromation to the database created by the create_talbes.py

template folder: store all the page html files
static folder: store all the images, javascript, and css files

Use flask to connect the back-end and the font-end to emulate the theater ticket system.
